
 validate and audit tha  the IA analisys is made by an agent :
 
 
  B concrete shape: three phases — setup (once), launch (per job), stream (per job). SETUP: create agent via POST /v1/agents with beta header managed-agents-2026-04-01 — body has model, system prompt (Role + FADP + citation rules + benefit-scan rubric), tools=['bash','file','web_search','web_fetch'], mcp_servers=[{url:'https://mcp.luis-legal.ch/swiss-law', name:'swiss-law'}, {url:'https://mcp.luis-legal.ch/contract-tools', name:'contract-tools'}]; create environment with packages=['python3','qdrant-client','lxml'] and network_allowlist=['data.fedlex.admin.ch','www.bj.admin.ch','be.ch','zh.ch','ge.ch','bger.ch']. Store agent_id + env_id. LAUNCH: POST /v1/sessions {agent_id, environment_id, metadata:{user_id, task_type:'benefit_scan'}}; returns session_id. STREAM: POST /v1/sessions/{session_id}/events with user_event containing the task brief and a reference to an uploaded Files API document (context-capsule); consume SSE endpoint /v1/sessions/{session_id}/stream until final event. Store final artifacts in Swiss VPS. CRITICAL: memory / outcomes / multi-agent are research preview — do NOT depend on them without requesting access.Claude Managed Agents harness. Four concepts per docs: Agent (model + system prompt + tools + MCP servers + skills, created once, referenced by ID), Environment (cloud container template with preinstalled packages and network rules), Session (a running instance bound to one task), Events (SSE stream of user turns, tool results, status updates). Workflow: (1) POST /agents to create the 'SwissLegalBenefitScanner' agent — Sonnet 4.5/Opus, system prompt = role + FADP + citation rules, tools = Bash + File + web_search + web_fetch, MCP servers = custom Qdrant-MCP + Fedlex-MCP endpoints; (2) POST /environments to define the container with python/psycopg/qdrant-client preinstalled and network egress allowlisted to data.fedlex.admin.ch, bger.ch, cantonal domains; (3) POST /sessions starts a run; (4) send events, stream SSE, fetch final artifact. Beta header: managed-agents-2026-04-01. This is the COLD PATH — hours-long benefit scans, cantonal sweeps, multi-article corpus reasoning. Rate limit: 60 create/min, 600 read/min per org.


 "Crucial entanglement: both configs share the SAME tool definitions and the SAME MCP backend. The six contract-law tools plus qdrant_search plus benefit_scan are defined once as MCP servers, then exposed to both configs. Config A consumes them via anthropic-python client `tools` parameter. Config B registers the same MCP servers in the Agent definition. This gives single-source-of-truth for tool logic — change the analyze_tort implementation in one place, both configs pick it up. Key architectural principle: keep LEGAL LOGIC in MCP servers; keep LLM ORCHESTRATION in the config-specific layer. Build three MCP servers: (1) swiss-law-retrieval-mcp (qdrant_search, fetch_article_by_sr, list_citations), (2) swiss-contract-tools-mcp (the six analysis tools), (3) swiss-user-context-mcp (read_user_docs, update_user_profile). All three are FastMCP Python servers runnable locally (Config A) or reachable via public HTTPS (Config B — Managed Agents needs remote MCP).
